# Current state observations

The hero still has 72px below viewport. The divider between subtitle and intro is visible but very subtle (line-dot-line pattern). The headline is slightly higher now but still has a lot of empty space above it between header and headline. The stats row is partially cut off at the bottom.

Need to reduce the empty space above the headline more aggressively to fit everything in viewport.
